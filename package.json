{
  "name": "may-designs",
  "version": "1.0.0",
  "private": true,
  "description": "MAY DESIGNS — handmade earrings and magnets. Static storefront built from src/ into a single self-contained index.html.",
  "scripts": {
    "build": "node build.js",
    "dev": "node dev.js",
    "test": "node build.js && node check.js",
    "check": "node check.js"
  },
  "engines": {
    "node": ">=18"
  }
}
